import { CountorderPipe } from './countorder.pipe';

describe('CountorderPipe', () => {
  it('create an instance', () => {
    const pipe = new CountorderPipe();
    expect(pipe).toBeTruthy();
  });
});
